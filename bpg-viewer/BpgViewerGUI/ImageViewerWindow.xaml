<Window x:Class="BpgViewer.ImageViewerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewModels="clr-namespace:BpgViewer.ViewModels"
        xmlns:views="clr-namespace:BpgViewer.Views"
        mc:Ignorable="d"
        Title="{Binding StatusMessage}"
        Height="600" Width="800"
        Background="#FFF5F5F5"
        WindowStartupLocation="CenterScreen">

    <Window.DataContext>
        <viewModels:ImageViewerViewModel/>
    </Window.DataContext>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- Menu -->
            <RowDefinition Height="*"/>     <!-- Image -->
            <RowDefinition Height="Auto"/> <!-- Status bar -->
        </Grid.RowDefinitions>

        <!-- Menu Bar -->
        <Menu Grid.Row="0" Background="#FFE8E8E8" Foreground="#FF333333">
            <MenuItem Header="File">
                <MenuItem Header="Open..." Command="{Binding OpenFileCommand}" InputGestureText="Ctrl+O"/>
                <Separator/>
                <MenuItem Header="Close" Click="Close_Click" InputGestureText="Esc"/>
            </MenuItem>
            <MenuItem Header="View">
                <MenuItem Header="Zoom In" Command="{Binding ZoomInCommand}" InputGestureText="+"/>
                <MenuItem Header="Zoom Out" Command="{Binding ZoomOutCommand}" InputGestureText="-"/>
                <Separator/>
                <MenuItem Header="Actual Size (100%)" Command="{Binding ActualSizeCommand}" InputGestureText="1"/>
                <MenuItem Header="Fit to Window" Command="{Binding FitToWindowCommand}" InputGestureText="0"/>
                <Separator/>
                <MenuItem Header="Show Info Panel"
                          IsCheckable="True"
                          IsChecked="{Binding IsInfoPanelVisible, Mode=TwoWay}"
                          Command="{Binding ToggleInfoPanelCommand}"/>
            </MenuItem>
        </Menu>

        <!-- Image Viewer Control with Overlay -->
        <Grid Grid.Row="1" x:Name="ImageViewerGrid">
            <views:ImageViewerControl/>

            <!-- Metadata Overlay - appears on hover in bottom-left area -->
            <Border x:Name="MetadataOverlay"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Bottom"
                    Margin="20"
                    Padding="12"
                    Background="#CC000000"
                    CornerRadius="8"
                    MaxWidth="400"
                    Visibility="Collapsed">
                <TextBlock Text="{Binding ImageInfo}"
                          Foreground="White"
                          FontSize="14"
                          FontWeight="SemiBold"
                          TextWrapping="Wrap"
                          LineHeight="20"/>
            </Border>

            <!-- Hover area in bottom-left corner to trigger metadata display -->
            <Border HorizontalAlignment="Left"
                    VerticalAlignment="Bottom"
                    Width="150" Height="150"
                    Background="Transparent"
                    MouseEnter="HoverArea_MouseEnter"
                    MouseLeave="HoverArea_MouseLeave"/>
        </Grid>

        <!-- Status Bar -->
        <Border Grid.Row="2"
               Background="#FFE8E8E8"
               BorderBrush="#FFD0D0D0"
               BorderThickness="0,1,0,0"
               Padding="10,5">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0"
                          Text="{Binding StatusMessage}"
                          Foreground="#FF555555"
                          VerticalAlignment="Center"/>

                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <TextBlock Text="{Binding ZoomPercentage}"
                              Foreground="#FF333333"
                              VerticalAlignment="Center"
                              Margin="0,0,10,0"
                              FontWeight="Bold"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
