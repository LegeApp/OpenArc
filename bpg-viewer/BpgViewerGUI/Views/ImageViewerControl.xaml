<UserControl x:Class="BpgViewer.Views.ImageViewerControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800"
             Background="#FFF5F5F5"
             MouseWheel="UserControl_MouseWheel"
             MouseLeftButtonDown="UserControl_MouseLeftButtonDown"
             MouseMove="UserControl_MouseMove"
             MouseLeftButtonUp="UserControl_MouseLeftButtonUp"
             Cursor="{Binding IsPanning, Converter={StaticResource PanningCursorConverter}, FallbackValue='Arrow'}">

    <Grid x:Name="RootGrid" Background="#FFF0F0F0">

        <!-- Image display area with ScrollViewer for large images -->
        <ScrollViewer x:Name="ImageScrollViewer"
                      HorizontalScrollBarVisibility="Auto"
                      VerticalScrollBarVisibility="Auto"
                      Background="#FFF0F0F0">

            <!-- The actual image - simple, no transforms -->
            <Image x:Name="DisplayImage"
                   Source="{Binding DisplayBitmap}"
                   RenderOptions.BitmapScalingMode="HighQuality"
                   Stretch="None">
                <Image.LayoutTransform>
                    <ScaleTransform ScaleX="{Binding ZoomLevel}"
                                    ScaleY="{Binding ZoomLevel}"/>
                </Image.LayoutTransform>
            </Image>
        </ScrollViewer>

        <!-- Empty state overlay -->
        <Grid Visibility="{Binding IsImageLoaded, Converter={StaticResource InverseBoolToVisibilityConverter}}">
            <StackPanel VerticalAlignment="Center"
                       HorizontalAlignment="Center">
                <TextBlock Text="📷"
                          FontSize="72"
                          HorizontalAlignment="Center"
                          Foreground="#FF999999"
                          Margin="0,0,0,20"/>

                <TextBlock Text="No image loaded"
                          FontSize="20"
                          Foreground="#FF666666"
                          HorizontalAlignment="Center"
                          Margin="0,0,0,10"/>

                <TextBlock Text="Click 'Open' or drag a BPG file here"
                          FontSize="14"
                          Foreground="#FF888888"
                          HorizontalAlignment="Center"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
