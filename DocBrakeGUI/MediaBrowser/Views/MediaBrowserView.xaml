<UserControl x:Class="DocBrake.MediaBrowser.Views.MediaBrowserView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vwp="clr-namespace:WpfToolkit.Controls;assembly=VirtualizingWrapPanel"
             xmlns:converters="clr-namespace:DocBrake.MediaBrowser.Converters"
             xmlns:coreConverters="clr-namespace:DocBrake.Converters"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="1000"
             Background="#FFF5F5F5"
             AllowDrop="True"
             Drop="UserControl_Drop"
             DragOver="UserControl_DragOver">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="FolderSelectionIndicatorResources.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <converters:NullToVisibilityConverter x:Key="NullToVisibilityConverter"/>
            <converters:ZeroToVisibilityConverter x:Key="ZeroToVisibilityConverter"/>
            <converters:NonZeroToVisibilityConverter x:Key="NonZeroToVisibilityConverter"/>
            <coreConverters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
            <coreConverters:InverseBoolToVisibilityConverter x:Key="InverseBoolToVisibilityConverter"/>

            <!-- Thumbnail item template - Wall-to-wall with overlaid text -->
            <DataTemplate x:Key="ThumbnailTemplate">
            <Border x:Name="ThumbnailBorder"
                    Margin="{Binding DataContext.ThumbnailMargin, RelativeSource={RelativeSource AncestorType=UserControl}}"
                    Background="#FF2A2A2A"
                    BorderBrush="Transparent"
                    BorderThickness="0"
                    Cursor="Hand"
                    ClipToBounds="True"
                    Width="{Binding DataContext.ThumbnailSize, RelativeSource={RelativeSource AncestorType=UserControl}}"
                    Height="{Binding DataContext.ThumbnailImageHeight, RelativeSource={RelativeSource AncestorType=UserControl}}"
                    >
                <Grid>
                    <!-- Loading indicator -->
                    <StackPanel x:Name="LoadingIndicator"
                               Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}"
                               VerticalAlignment="Center"
                               HorizontalAlignment="Center">
                        <TextBlock Text="⏳"
                                  FontSize="24"
                                  HorizontalAlignment="Center"
                                  Foreground="#FF888888"/>
                    </StackPanel>

                    <!-- Error indicator -->
                    <StackPanel Visibility="{Binding HasError, Converter={StaticResource BoolToVisibilityConverter}}"
                               VerticalAlignment="Center"
                               HorizontalAlignment="Center">
                        <TextBlock Text="❌"
                                  FontSize="24"
                                  HorizontalAlignment="Center"
                                  Foreground="#FFCC6666"/>
                    </StackPanel>

                    <!-- Actual thumbnail - UniformToFill for wall-to-wall -->
                    <Image Source="{Binding ThumbnailImage}"
                          Stretch="UniformToFill"
                          RenderOptions.BitmapScalingMode="HighQuality"
                          Visibility="{Binding ThumbnailImage, Converter={StaticResource NullToVisibilityConverter}}"
                          MouseLeftButtonDown="ThumbnailImage_MouseLeftButtonDown"/>

                    <!-- Stats mode card (when thumbnails are disabled) -->
                    <Border x:Name="StatsCard"
                           Visibility="{Binding IsStatsMode, Converter={StaticResource BoolToVisibilityConverter}}"
                           Background="{Binding ExtensionColor}"
                           Padding="8"
                           MouseLeftButtonDown="ThumbnailImage_MouseLeftButtonDown">
                        <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                            <TextBlock Text="{Binding StatsText}"
                                      Foreground="{Binding ExtensionTextColor}"
                                      FontSize="9"
                                      FontFamily="Consolas, Courier New"
                                      TextWrapping="Wrap"
                                      LineHeight="14"/>
                        </ScrollViewer>
                    </Border>

                    <!-- Checkbox overlay in top-left -->
                    <CheckBox x:Name="ThumbCheckBox"
                              IsChecked="{Binding IsChecked, Mode=TwoWay}"
                              VerticalAlignment="Top"
                              HorizontalAlignment="Left"
                              Margin="4,4,0,0"
                              Click="Checkbox_Click">
                        <CheckBox.Style>
                            <Style TargetType="CheckBox">
                                <Setter Property="Background" Value="#80000000"/>
                            </Style>
                        </CheckBox.Style>
                    </CheckBox>

                    <!-- Overlaid text panel at bottom - hides on mouseover -->
                    <Border x:Name="OverlayTextPanel"
                            VerticalAlignment="Bottom"
                            HorizontalAlignment="Stretch"
                            Background="{Binding ExtensionColor}"
                            Opacity="0.9"
                            Padding="6,4">
                        <StackPanel>
                            <TextBlock Foreground="{Binding ExtensionTextColor}"
                                      FontSize="10"
                                      FontWeight="SemiBold"
                                      TextTrimming="CharacterEllipsis"
                                      TextAlignment="Left"
                                      ToolTip="{Binding FileName}"
                                      MouseLeftButtonDown="Filename_MouseLeftButtonDown">
                                <Run Text="{Binding FileNameWithoutExtension, Mode=OneWay}"/>
                                <Run Text="{Binding FileExtension, Mode=OneWay}"/>
                            </TextBlock>
                            <TextBlock Foreground="{Binding ExtensionTextColor}"
                                      FontSize="9"
                                      Opacity="0.8"
                                      TextAlignment="Left">
                                <Run Text="{Binding FileSizeFormatted, Mode=OneWay}"/>
                            </TextBlock>
                        </StackPanel>
                    </Border>

                    <!-- Selection border overlay -->
                    <Border x:Name="SelectionBorder"
                            BorderBrush="Transparent"
                            BorderThickness="0"
                            IsHitTestVisible="False"/>
                </Grid>
            </Border>

            <DataTemplate.Triggers>
                <DataTrigger Binding="{Binding IsSelected}" Value="True">
                    <Setter TargetName="SelectionBorder" Property="BorderBrush" Value="#FF007ACC"/>
                    <Setter TargetName="SelectionBorder" Property="BorderThickness" Value="3"/>
                </DataTrigger>
                
                <!-- Show by default mode: Show normally, hide on mouseover -->
                <MultiDataTrigger>
                    <MultiDataTrigger.Conditions>
                        <Condition Binding="{Binding RelativeSource={RelativeSource AncestorType=UserControl}, Path=DataContext.ShowThumbnailLabelsByDefault}" Value="True"/>
                        <Condition Binding="{Binding ElementName=ThumbnailBorder, Path=IsMouseOver}" Value="True"/>
                    </MultiDataTrigger.Conditions>
                    <Setter TargetName="OverlayTextPanel" Property="Visibility" Value="Collapsed"/>
                </MultiDataTrigger>
                
                <!-- Hide by default mode: Hide normally, show on mouseover -->
                <MultiDataTrigger>
                    <MultiDataTrigger.Conditions>
                        <Condition Binding="{Binding RelativeSource={RelativeSource AncestorType=UserControl}, Path=DataContext.ShowThumbnailLabelsByDefault}" Value="False"/>
                        <Condition Binding="{Binding ElementName=ThumbnailBorder, Path=IsMouseOver}" Value="False"/>
                    </MultiDataTrigger.Conditions>
                    <Setter TargetName="OverlayTextPanel" Property="Visibility" Value="Collapsed"/>
                </MultiDataTrigger>
            </DataTemplate.Triggers>
            </DataTemplate>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Toolbar -->
            <RowDefinition Height="*"/>
            <!-- Thumbnails -->
            <RowDefinition Height="Auto"/>
            <!-- Status bar -->
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <Border Grid.Row="0"
               Background="#FFE8E8E8"
               BorderBrush="#FFD0D0D0"
               BorderThickness="0,0,0,1"
               Padding="10,8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Left buttons -->
                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <Button Content=" Open File"
                           Command="{Binding OpenFileCommand}"
                           Padding="10,5"
                           Margin="0,0,5,0"
                           Background="#FFDDDDDD"
                           Foreground="#FF333333"
                           BorderThickness="1"
                           BorderBrush="#FFC0C0C0"/>

                    <Button Content=" Open Folder"
                           Command="{Binding OpenFolderCommand}"
                           Padding="10,5"
                           Margin="0,0,5,0"
                           Background="#FFDDDDDD"
                           Foreground="#FF333333"
                           BorderThickness="1"
                           BorderBrush="#FFC0C0C0"/>

                    <Button Content=" Refresh"
                           Command="{Binding RefreshCommand}"
                           Padding="10,5"
                           Margin="0,0,5,0"
                           Background="#FFDDDDDD"
                           Foreground="#FF333333"
                           BorderThickness="1"
                           BorderBrush="#FFC0C0C0"/>

                    <Separator Width="1" Background="#FFD0D0D0" Margin="10,0"/>

                    <Grid VerticalAlignment="Center">
                        <TextBlock x:Name="DirectoryNameText"
                                  Text="{Binding DirectoryName}"
                                  Foreground="#FF555555"
                                  VerticalAlignment="Center"
                                  FontSize="12"
                                  MaxWidth="400"
                                  TextTrimming="CharacterEllipsis"
                                  ToolTip="{Binding CurrentDirectory}"
                                  MouseLeftButtonDown="DirectoryName_MouseLeftButtonDown"/>

                        <TextBox x:Name="DirectoryPathTextBox"
                                 Text="{Binding CurrentDirectory, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                 Foreground="#FF333333"
                                 Background="#FFFFFFFF"
                                 BorderBrush="#FF007ACC"
                                 BorderThickness="1"
                                 Padding="4,2"
                                 FontSize="12"
                                 Width="420"
                                 Visibility="Collapsed"
                                 KeyDown="DirectoryPathTextBox_KeyDown"
                                 LostFocus="DirectoryPathTextBox_LostFocus"/>
                    </Grid>

                    <Separator Width="1" Background="#FFD0D0D0" Margin="10,0"/>

                    <!-- Selection controls -->
                    <Button Content=" Select All"
                           Command="{Binding SelectAllCommand}"
                           Padding="10,5"
                           Margin="0,0,5,0"
                           Background="#FFDDDDDD"
                           Foreground="#FF333333"
                           BorderThickness="1"
                           BorderBrush="#FFC0C0C0"/>

                    <Button Content=" Clear"
                           Command="{Binding ClearSelectionCommand}"
                           Padding="10,5"
                           Margin="0,0,5,0"
                           Background="#FFDDDDDD"
                           Foreground="#FF333333"
                           BorderThickness="1"
                           BorderBrush="#FFC0C0C0"/>

                    <TextBlock Text="{Binding SelectionSummary}"
                              Foreground="#FF007ACC"
                              FontWeight="SemiBold"
                              VerticalAlignment="Center"
                              Margin="10,0,0,0"/>
                </StackPanel>

                <!-- Right controls -->
                <StackPanel Grid.Column="2" Orientation="Horizontal">
                    <TextBlock Text="Size:"
                              Foreground="#FF555555"
                              VerticalAlignment="Center"
                              Margin="0,0,5,0"/>

                    <Slider Minimum="80"
                            Maximum="600"
                            Value="{Binding ThumbnailSize, Mode=TwoWay}"
                            Width="100"
                            Height="22"
                            IsSnapToTickEnabled="True"
                            TickFrequency="10"
                            Background="#FFE0E0E0"/>

                    <TextBlock Text="Gap:"
                              Foreground="#FF555555"
                              VerticalAlignment="Center"
                              Margin="10,0,5,0"/>

                    <Slider Minimum="0"
                            Maximum="20"
                            Value="{Binding ThumbnailPadding, Mode=TwoWay}"
                            Width="60"
                            Height="22"
                            IsSnapToTickEnabled="True"
                            TickFrequency="1"
                            Background="#FFE0E0E0"
                            ToolTip="Spacing between thumbnails (0 = wall-to-wall)"/>

                    <Separator Width="1" Background="#FFD0D0D0" Margin="10,0"/>

                    <Button Content="Details"
                            Command="{Binding ToggleDetailsViewCommand}"
                            Padding="10,5"
                            Margin="0,0,5,0"
                            Background="#FFDDDDDD"
                            Foreground="#FF333333"
                            BorderThickness="1"
                            BorderBrush="#FFC0C0C0"/>

                    <Separator Width="1" Background="#FFD0D0D0" Margin="10,0"/>

                    <Button Content=" Clear Cache"
                           Command="{Binding ClearCacheCommand}"
                           Padding="10,5"
                           Background="#FFDDDDDD"
                           Foreground="#FF333333"
                           BorderThickness="1"
                           BorderBrush="#FFC0C0C0"
                           ToolTip="{Binding CacheSizeFormatted, StringFormat='Cache size: {0}'}"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Main Content Area -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="220"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- File Browser Sidebar -->
            <TreeView Grid.Column="0" 
                      ItemsSource="{Binding RootFolders}"
                      BorderThickness="0"
                      Background="#FFF0F0F0"
                      Padding="5"
                      SelectedItemChanged="TreeView_SelectedItemChanged">
                <TreeView.ItemTemplate>
                    <HierarchicalDataTemplate ItemsSource="{Binding SubFolders}">
                        <StackPanel Orientation="Horizontal" Margin="0,2">
                            <Border x:Name="FolderIconBorder"
                                    Width="18"
                                    Height="18"
                                    Margin="0,0,6,0"
                                    BorderBrush="Transparent"
                                    BorderThickness="1"
                                    CornerRadius="2"
                                    Background="Transparent"
                                    MouseLeftButtonDown="FolderIcon_MouseLeftButtonDown">
                                <TextBlock Text="{Binding Icon}" Foreground="#FF666666" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                            </Border>
                            <TextBlock x:Name="FolderNameText" Text="{Binding Name}" Foreground="#FF333333"/>
                        </StackPanel>
                        <HierarchicalDataTemplate.Triggers>
                            <DataTrigger Binding="{Binding IsChecked}" Value="True">
                                <Setter TargetName="FolderIconBorder" Property="BorderBrush" Value="{StaticResource FolderSelectionSelectedBorderBrush}"/>
                                <Setter TargetName="FolderIconBorder" Property="BorderThickness" Value="2"/>
                            </DataTrigger>
                            <MultiDataTrigger>
                                <MultiDataTrigger.Conditions>
                                    <Condition Binding="{Binding IsChecked}" Value="False"/>
                                    <Condition Binding="{Binding HasExplicitCheckState}" Value="True"/>
                                </MultiDataTrigger.Conditions>
                                <Setter TargetName="FolderIconBorder" Property="BorderBrush" Value="{StaticResource FolderSelectionIgnoredBorderBrush}"/>
                                <Setter TargetName="FolderIconBorder" Property="BorderThickness" Value="2"/>
                            </MultiDataTrigger>
                            <DataTrigger Binding="{Binding IsChecked}" Value="{x:Null}">
                                <Setter TargetName="FolderIconBorder" Property="BorderBrush" Value="{StaticResource FolderSelectionPartialBorderBrush}"/>
                                <Setter TargetName="FolderIconBorder" Property="BorderThickness" Value="2"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding IsDcimFolder}" Value="True">
                                <Setter TargetName="FolderNameText" Property="TextBlock.FontWeight" Value="SemiBold"/>
                                <Setter TargetName="FolderNameText" Property="TextBlock.Foreground" Value="#FF007ACC"/>
                            </DataTrigger>
                        </HierarchicalDataTemplate.Triggers>
                    </HierarchicalDataTemplate>
                </TreeView.ItemTemplate>
                <TreeView.ItemContainerStyle>
                    <Style TargetType="TreeViewItem">
                        <Setter Property="IsExpanded" Value="{Binding IsExpanded, Mode=TwoWay}"/>
                        <Setter Property="IsSelected" Value="{Binding IsSelected, Mode=TwoWay}"/>
                    </Style>
                </TreeView.ItemContainerStyle>
            </TreeView>

            <!-- Splitter -->
            <GridSplitter Grid.Column="1" 
                         Width="3" 
                         HorizontalAlignment="Center" 
                         VerticalAlignment="Stretch"
                         Background="#FFE0E0E0"/>

            <!-- Content (Thumbnails or Details) -->
            <Grid Grid.Column="2"
                  x:Name="ThumbnailScroller"
                  Background="#FFF5F5F5"
                  PreviewKeyDown="MediaBrowserView_KeyDown"
                  Focusable="True">

                <Button Content="Show Prepared"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Top"
                        Margin="0,8,8,0"
                        Padding="10,4"
                        Command="{Binding ToggleQueuePanelCommand}"
                        Visibility="{Binding IsQueuePanelVisible, Converter={StaticResource InverseBoolToVisibilityConverter}}"/>
                    <StackPanel Visibility="{Binding TotalCount, Converter={StaticResource ZeroToVisibilityConverter}}"
                               VerticalAlignment="Center"
                               HorizontalAlignment="Center"
                               Margin="50">
                    <TextBlock Text=" "
                              FontSize="72"
                              HorizontalAlignment="Center"
                              Foreground="#FF999999"/>
                    <TextBlock Text="No files"
                              FontSize="20"
                              HorizontalAlignment="Center"
                              Foreground="#FF666666"
                              Margin="0,10,0,0"/>
                    <TextBlock Text="Select a folder or drag files here"
                              FontSize="12"
                              HorizontalAlignment="Center"
                              Foreground="#FF888888"
                              Margin="0,5,0,0"/>
                </StackPanel>

                <!-- Thumbnails ListView with VirtualizingWrapPanel -->
                <ListView x:Name="ThumbnailsList"
                         ItemsSource="{Binding Thumbnails}"
                         ItemTemplate="{StaticResource ThumbnailTemplate}"
                         HorizontalAlignment="Stretch"
                         HorizontalContentAlignment="Stretch"
                         Background="Transparent"
                         BorderThickness="0"
                         Padding="0"
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                         ScrollViewer.VerticalScrollBarVisibility="Auto"
                         VirtualizingPanel.IsVirtualizing="True"
                         VirtualizingPanel.VirtualizationMode="Recycling"
                         SelectionMode="Extended"
                         SelectionChanged="ItemsList_SelectionChanged"
                         PreviewMouseRightButtonDown="ItemsList_PreviewMouseRightButtonDown"
                         PreviewMouseMove="ItemsList_PreviewMouseMove"
                         KeyDown="ItemsList_KeyDown"
                         Visibility="{Binding IsDetailsView, Converter={StaticResource InverseBoolToVisibilityConverter}}">
                    <ListView.ContextMenu>
                        <ContextMenu>
                            <MenuItem Header="Add to queue" Click="ContextMenu_AddToQueue_Click"/>
                        </ContextMenu>
                    </ListView.ContextMenu>
                    <ListView.ItemsPanel>
                        <ItemsPanelTemplate>
                            <vwp:VirtualizingWrapPanel Orientation="Horizontal"
                                                      SpacingMode="None"
                                                      StretchItems="False"/>
                        </ItemsPanelTemplate>
                    </ListView.ItemsPanel>
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="Padding" Value="0"/>
                            <Setter Property="Margin" Value="0"/>
                            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                            <Setter Property="VerticalContentAlignment" Value="Stretch"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListViewItem">
                                        <ContentPresenter/>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ListView.ItemContainerStyle>
                </ListView>

                    <!-- Details ListView (Explorer-style) -->
                    <ListView x:Name="DetailsList"
                             ItemsSource="{Binding Thumbnails}"
                             Background="Transparent"
                             BorderThickness="0"
                             SelectionMode="Extended"
                             SelectionChanged="ItemsList_SelectionChanged"
                             PreviewMouseRightButtonDown="ItemsList_PreviewMouseRightButtonDown"
                             PreviewMouseMove="ItemsList_PreviewMouseMove"
                             KeyDown="ItemsList_KeyDown"
                             Visibility="{Binding IsDetailsView, Converter={StaticResource BoolToVisibilityConverter}}"
                             MouseDoubleClick="DetailsList_MouseDoubleClick">
                        <ListView.ContextMenu>
                            <ContextMenu>
                                <MenuItem Header="Add to queue" Click="ContextMenu_AddToQueue_Click"/>
                            </ContextMenu>
                        </ListView.ContextMenu>
                        <ListView.View>
                            <GridView>
                                <GridViewColumn Width="28">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <CheckBox IsChecked="{Binding IsChecked, Mode=TwoWay}"
                                                      VerticalAlignment="Center"
                                                      Click="Checkbox_Click"/>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                <GridViewColumn Width="320">
                                    <GridViewColumn.Header>
                                        <GridViewColumnHeader Content="Name" Tag="FileName"/>
                                    </GridViewColumn.Header>
                                    <GridViewColumn.DisplayMemberBinding>
                                        <Binding Path="FileName"/>
                                    </GridViewColumn.DisplayMemberBinding>
                                </GridViewColumn>
                                <GridViewColumn Width="160">
                                    <GridViewColumn.Header>
                                        <GridViewColumnHeader Content="Date Modified" Tag="LastModified"/>
                                    </GridViewColumn.Header>
                                    <GridViewColumn.DisplayMemberBinding>
                                        <Binding Path="LastModified" StringFormat="{}{0:yyyy-MM-dd HH:mm}"/>
                                    </GridViewColumn.DisplayMemberBinding>
                                </GridViewColumn>
                                <GridViewColumn Width="110">
                                    <GridViewColumn.Header>
                                        <GridViewColumnHeader Content="Type" Tag="FileExtension"/>
                                    </GridViewColumn.Header>
                                    <GridViewColumn.DisplayMemberBinding>
                                        <Binding Path="FileExtension"/>
                                    </GridViewColumn.DisplayMemberBinding>
                                </GridViewColumn>
                                <GridViewColumn Width="110">
                                    <GridViewColumn.Header>
                                        <GridViewColumnHeader Content="Size" Tag="FileSize"/>
                                    </GridViewColumn.Header>
                                    <GridViewColumn.DisplayMemberBinding>
                                        <Binding Path="FileSizeFormatted"/>
                                    </GridViewColumn.DisplayMemberBinding>
                                </GridViewColumn>
                            </GridView>
                        </ListView.View>
                    </ListView>
            </Grid>

            <GridSplitter Grid.Column="3"
                         Width="4"
                         HorizontalAlignment="Center"
                         VerticalAlignment="Stretch"
                         Background="#FFE0E0E0"
                         Visibility="{Binding IsQueuePanelVisible, Converter={StaticResource BoolToVisibilityConverter}}"/>

            <Border Grid.Column="4"
                    Width="320"
                    Background="#FFF0F0F0"
                    BorderBrush="#FFD0D0D0"
                    BorderThickness="1,0,0,0"
                    Visibility="{Binding IsQueuePanelVisible, Converter={StaticResource BoolToVisibilityConverter}}">
                <Grid AllowDrop="True"
                      DragOver="QueuePanel_DragOver"
                      Drop="QueuePanel_Drop">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0" Padding="10,8" Background="#FFE8E8E8" BorderBrush="#FFD0D0D0" BorderThickness="0,0,0,1">
                        <TextBlock Foreground="#FF333333" FontWeight="SemiBold">
                            <Run Text="Processing Queue"/>
                            <Run Text=" ("/>
                            <Run Text="{Binding QueueItemCount, Mode=OneWay}"/>
                            <Run Text=")"/>
                        </TextBlock>
                    </Border>

                    <ListView Grid.Row="1"
                             ItemsSource="{Binding QueueItems}"
                             Background="Transparent"
                             BorderThickness="0">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" Margin="6,4">
                                    <TextBlock Text="{Binding FileName}" Foreground="#FF333333" TextTrimming="CharacterEllipsis" />
                                </StackPanel>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>

                    <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="10,8">
                        <Button Content="Clear Queue" Padding="12,4" Command="{Binding ClearQueueCommand}"/>
                    </StackPanel>
                </Grid>
            </Border>

        </Grid>

        <!-- Status bar -->
        <Border Grid.Row="2"
               Background="#FFE8E8E8"
               BorderBrush="#FFD0D0D0"
               BorderThickness="0,1,0,0"
               Padding="10,5">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0"
                          Text="{Binding StatusMessage}"
                          Foreground="#FF555555"
                          VerticalAlignment="Center"/>

                <!-- Loading progress (only visible when loading) -->
                <StackPanel Grid.Column="1" Orientation="Horizontal" 
                           Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}"
                           Margin="0,0,15,0">
                    <ProgressBar Width="100" Height="12" 
                                Value="{Binding LoadingProgress, Mode=OneWay}" Maximum="100"
                                Margin="0,0,8,0"/>
                    <TextBlock Foreground="#FF555555" VerticalAlignment="Center" FontSize="11">
                        <Run Text="{Binding LoadedCount, Mode=OneWay}"/><Run Text="/"/><Run Text="{Binding TotalCount, Mode=OneWay}"/>
                    </TextBlock>
                </StackPanel>

                <TextBlock Grid.Column="2"
                          Foreground="#FF888888"
                          VerticalAlignment="Center">
                    <Run Text="{Binding TotalCount, Mode=OneWay}"/>
                    <Run Text="files"/>
                </TextBlock>
            </Grid>
        </Border>
    </Grid>
</UserControl>
