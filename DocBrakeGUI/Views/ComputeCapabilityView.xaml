<UserControl x:Class="DocBrake.Views.ComputeCapabilityView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:viewmodels="clr-namespace:DocBrake.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="30" d:DesignWidth="150"
             d:DataContext="{d:DesignInstance Type=viewmodels:ComputeCapabilityViewModel}">
    
    <UserControl.Resources>
        <Style x:Key="CapabilityTextBlockStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Margin" Value="3,0"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Opacity" Value="0.6"/>
            <Setter Property="Foreground" Value="Gray"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsAvailable}" Value="True">
                    <Setter Property="Foreground" Value="Green"/>
                    <Setter Property="Opacity" Value="0.75"/>
                    <Setter Property="FontWeight" Value="SemiBold"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Type}" Value="{x:Static viewmodels:ComputeType.Cuda}">
                    <Setter Property="ToolTip" Value="CUDA Acceleration"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Type}" Value="{x:Static viewmodels:ComputeType.OpenCL}">
                    <Setter Property="ToolTip" Value="OpenCL Acceleration"/>
                </DataTrigger>

                <DataTrigger Binding="{Binding Type}" Value="{x:Static viewmodels:ComputeType.CPU}">
                    <Setter Property="ToolTip" Value="CPU Processing"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>
    
    <Border Background="Transparent" 
            ToolTip="Click to toggle view" 
            MouseLeftButtonDown="Border_MouseLeftButtonDown">
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <!-- Display all options when ShowAllOptions is true -->
            <ItemsControl ItemsSource="{Binding AvailableCapabilities}" 
                          Visibility="{Binding ShowAllOptions, Converter={StaticResource BoolToVisibilityConverter}}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding DisplayName}" 
                                   Style="{StaticResource CapabilityTextBlockStyle}"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
            
            <!-- Display only selected option when ShowAllOptions is false -->
            <TextBlock Text="{Binding SelectedCapability.DisplayName}" 
                       Style="{StaticResource CapabilityTextBlockStyle}"
                       Visibility="{Binding ShowAllOptions, Converter={StaticResource InverseBoolToVisibilityConverter}}"/>
        </StackPanel>
    </Border>
</UserControl>
