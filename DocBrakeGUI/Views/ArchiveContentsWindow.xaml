<Window x:Class="DocBrake.Views.ArchiveContentsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Archive Contents"
        Height="600"
        Width="900"
        MinHeight="400"
        MinWidth="650"
        WindowStartupLocation="CenterOwner">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,8">
            <Button Content="Refresh" Command="{Binding RefreshCommand}" MinWidth="90" Margin="0,0,8,0" />
            <Button Content="Open" Command="{Binding OpenSelectedCommand}" MinWidth="90" />
        </StackPanel>

        <DataGrid Grid.Row="1"
                  ItemsSource="{Binding Files}"
                  SelectedItem="{Binding SelectedFile}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Path" Binding="{Binding Filename}" Width="*" />
                <DataGridTextColumn Header="Type" Binding="{Binding FileType}" Width="90" />
                <DataGridTextColumn Header="Original" Binding="{Binding FormattedOriginalSize}" Width="110" />
                <DataGridTextColumn Header="Stored" Binding="{Binding FormattedCompressedSize}" Width="110" />
                <DataGridTextColumn Header="%" Binding="{Binding CompressionPercentage}" Width="70" />
            </DataGrid.Columns>
        </DataGrid>

        <TextBlock Grid.Row="2" Text="{Binding StatusMessage}" Margin="0,8,0,0" />
    </Grid>
</Window>
