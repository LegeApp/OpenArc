<Window x:Class="DocBrake.Views.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:views="clr-namespace:DocBrake.Views"
        Title="Settings"
        Height="680"
        Width="1100"
        MinHeight="600"
        MinWidth="900"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        Loaded="SettingsWindow_Loaded">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Removed ScrollViewer - window now sizes to content -->
        <views:SettingsView Margin="10" />

        <Border Grid.Row="1" BorderThickness="0,1,0,0" Padding="12,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0"
                        Content="Reset Defaults"
                        Command="{Binding ResetToDefaultsCommand}"
                        MinWidth="120"
                        HorizontalAlignment="Left" />

                <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="Cancel"
                            Click="CancelButton_Click"
                            MinWidth="90" Margin="0,0,8,0"/>
                    <Button Content="OK"
                            Click="OkButton_Click"
                            MinWidth="90"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>

